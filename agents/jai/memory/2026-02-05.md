# 2026-02-05 Session Notes

## Auth Configuration (Main Focus Today)

### Problem
- Hit 401 "Invalid bearer token" error on `anthropic:manual` profile
- Token had expired, caused 19min cooldown

### Fixed
1. Generated new setup-token via `claude setup-token` (valid 1 year)
2. Pasted into Clawdbot: `clawdbot models auth paste-token --provider anthropic`
3. Set auth order to prioritize token over API key:
   ```bash
   clawdbot models auth order set --provider anthropic anthropic:manual anthropic:default
   ```

### Current Auth Setup
- **Primary:** `anthropic:manual` (token auth - free tier)
- **Fallback:** `anthropic:default` (API key - paid)
- Token valid until ~Feb 2027

### If Token Issues Recur
1. Run `claude setup-token` to generate new token
2. Run `clawdbot models auth paste-token --provider anthropic`
3. Token lasts 1 year

## MyJunto Status (From Yesterday)
- Newsletter ingestion system built but needs `SUPABASE_SERVICE_ROLE_KEY` fixed in Vercel
- Jon still needs to update that key (JWT starting with `eyJ...`)

## Pending Items
- MyJunto: Fix Supabase key in Vercel, test newsletter ingestion
- Almanack Dashboard: Builder agent in progress (task `almanack-build-2026-02-05`)

## Files Changed Today
- Auth profiles updated in `~/.clawdbot/agents/main/agent/auth-profiles.json`
