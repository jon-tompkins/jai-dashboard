## Clawstreet Position Sync & Prize Pool (20:18-21:46 UTC)

### Position Sync Trigger
- **Problem**: Trades and positions tables could get out of sync (separate inserts)
- **Solution**: Postgres trigger `sync_position_on_trade` that fires AFTER INSERT on trades
  - OPEN → creates position automatically
  - CLOSE → deletes position automatically
- **Migration**: `migrations/004_position_sync_trigger.sql`
- Removed manual position INSERT/DELETE from trade API
- Reordered trade API: insert trade first, then read positions

### Prize Pool Fix
- **Issue**: `select('column_name')` returning empty results from Supabase
- **Fix**: Use `select('*')` instead — some quirk with column-specific selects
- Now tracking:
  - Decay: 100 LOBS/agent/day (cron at 00:00 UTC)
  - Fees: 0.2% per trade stored in `trades.fee_lobs`
- Prize pool API working: 700 LOBS (600 decay + 100 fees)

### Decay System
- Created `decay_history` table
- Added `last_decay_date` to agents
- Function `apply_daily_decay()` — had to rename return columns to `out_agent_id` etc to avoid ambiguity
- Cron job set for 00:00 UTC daily

### Trollbox & Agent Activity
- Updated cron to include trollbox commentary
- Agents posting in-character about positions
- Posted initial messages from all 3 QA agents

### Hidden Trades (Commit-Reveal)
- Registered wallet for Contrarian-QA: `0xC1558D906d0D63B7F4879d34FDe93c5b0C459b57`
- Private key saved in `~/clawstreet/agents/contrarian-qa.json`
- Submitted 2 hidden trades (AAPL long, COIN short) — unrevealed

### Docs Updated
- `/docs` page rewritten with OPEN/CLOSE actions
- Added wallet registration steps
- Added commit-reveal documentation
- Added economics section (decay, fees, prize pool)

### Current State
- 6 active agents
- 3 QA agents with positions (reset to fresh today)
- Trigger syncing positions ✓
- Prize pool tracking ✓
- Decay collecting ✓
- Debug logging left in prize-pool API (cleanup before prod)
