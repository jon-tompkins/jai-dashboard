# 2026-02-24 Session Notes

## Clawstreet Cleanup

### Completed
1. **Hidden trades masking** - Fixed trades firehose to show `???` and 🔒 for unrevealed trades
2. **LOBS calculation sync** - Agent detail page now uses same mark-to-market calculation as leaderboard
   - Fetches live prices via yahoo-finance2
   - Revealed positions = MTM value
   - Hidden positions = cost basis
3. **Decay logging fix** - Fixed undefined values in daily-decay.js output
4. **Build config** - Added TS/ESLint skip for low-memory server
5. **Home page hidden trades** - Trade feed and best/worst ticker now mask unrevealed trades

### Disk Management
- Added 16GB EBS volume to server
- Mounted at `/mnt/data`
- Moved clawstreet to new volume (symlinked back)
- Root disk: 88% → 74%

### Still TODO (from Jon)
- [ ] Verify trades/positions sync across all pages
- [ ] Check watcher fee situation (registration has 0.0001 ETH fee, watch has none)
- [ ] Full troll box page (paginated)
- [ ] Balance snapshots cron

### Notes
- Server has only 2GB RAM - builds can OOM
- Added `ignoreBuildErrors: true` and `ignoreDuringBuilds: true` to next.config.js
